% --- Configuración Inicial ---
\documentclass[$if(fontsize)$$fontsize$$else$12pt$endif$,$if(lang)$$lang$$endif$]{article}

% Paquetes esenciales para compatibilidad
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
$if(lang)$
\usepackage[$lang$]{babel}
$endif$
\usepackage{lmodern}
\usepackage{geometry}
\geometry{verbose,tmargin=3cm,bmargin=3cm,lmargin=2.5cm,rmargin=2.5cm}

% --- Soporte para imágenes ---
\usepackage{graphicx}
% Esto asegura que las imágenes no se salgan de los márgenes
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth}

% --- Para listas ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\usepackage{enumitem}
\setlist[itemize]{noitemsep, topsep=0pt}
\setlist[enumerate]{noitemsep, topsep=0pt}

% --- Soporte para Tablas (Añade esto) ---
\usepackage{longtable} % Para tablas que ocupan más de una página
\usepackage{booktabs}  % Para líneas horizontales elegantes (\toprule, \midrule, etc.)
\usepackage{array}     % Para un mejor manejo de columnas y anchos

% --- Hipervínculos ---
\usepackage{hyperref}
\hypersetup{
  pdftitle={$title-meta$},
  pdfauthor={$author-meta$},
  colorlinks=true,
  linkcolor=blue,
  urlcolor=cyan,
  breaklinks=true
}

% --- Para completo soporte de todas las herramientas matemáticas que empleo 
\usepackage{actuarialangle}
\usepackage{actuarialsymbol}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tikz}

% --- Metadatos (Título, Autor, Fecha) ---
$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
\author{Victor Gabriel Vazquez Montes $for(author)$$author$$sep$ \and $endfor$}
$endif$

\date{$if(date)$$date$$else$\today$endif$}

% --- Inicio del Documento ---
\begin{document}

\begin{titlepage}
\centering
{\includegraphics[width=0.2\textwidth]{Logo_neutral_de_la_Universidad_Cristóbal_Colón.png}\par}
\vspace{1cm}
{\bfseries\LARGE Universidad Cristóbal Colón \par}
\vspace{1cm}
{\scshape\Large Licenciatura en Actuaría \par}
\vspace{1cm}
{\itshape\Large $if(tipo)$$tipo$$else$ Tipo de proyecto $endif$ \par}
\vspace{3cm}
{\scshape\Huge $if(title)$$title$$else$ T\'itulo del proyecto $endif$ \par}
\vspace{3cm}
$if(materia)$
{\Large $materia$ \par}
$endif$	
\vfill
$if(docente)$
{\Large Docente: $docente$ \par}
$endif$
\vfill
{\Large Autor: \par}
{\Large Victor Gabriel Vázquez Montes \par}
\vfill
{\Large $if(date)$$date$$else$\today $endif$ \par}
\end{titlepage}

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

% --- Para un toc mejorado (elimina el color azul de los hipervínculos)
$if(toc)$
{
  \hypersetup{linkcolor=black} % Cambiamos el color a negro solo aquí
  \tableofcontents
}
\newpage
$endif$

% --- EL CONTENIDO (Obligatorio) ---
$body$

\end{document}
